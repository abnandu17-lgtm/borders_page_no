# PDF Formatter

Streamlit app that formats uploaded PDFs with a safe content area, four-side border, project title, department footer and page numbers.

## Run

```bash
pip install -r requirements.txt
streamlit run app.py
```
